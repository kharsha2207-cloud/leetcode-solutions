Link:
https://leetcode.com/problems/reverse-string/

### Approach

I used two pointers, one at the beginning and one at the end of the string. The characters are swapped while the pointers move toward the center.

### Complexity

- Time: O(n)
- Space: O(1)

### Notes

I tested a normal string and a single-character string. A single-character string remains unchanged.